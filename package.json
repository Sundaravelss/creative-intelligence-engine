{
  "name": "creative-intelligence-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Agentic Marketing OS — node-based canvas + agent layer + recurring routines + connectors hub",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18.18.0",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.12.0"
}
